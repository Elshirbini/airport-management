# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Notification {
  id: ID!
  userId: ID!
  bookingId: ID
  flightId: ID
  type: NotificationType!
  channel: NotificationChannel!
  status: NotificationStatus!
  subject: String
  message: String!
  isRead: Boolean!
  readAt: DateTime
  sentAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum NotificationType {
  BOOKING_CONFIRMATION
  FLIGHT_DELAY
  WELCOME
  OTP_CONFIRMATION
  RESET_PASSWORD
}

enum NotificationChannel {
  DATABASE
  SOCKET
  EMAIL
  SMS
  GRAPHQL_PUBSUB
}

enum NotificationStatus {
  PENDING
  SENT
  FAILED
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type PaginationMeta {
  page: Int!
  totalCount: Int!
}

type NotificationResponse {
  notifications: [Notification!]!
  meta: PaginationMeta!
}

type Airport {
  id: ID!
  name: String!
  code: String!
  city: String!
  country: String!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type AirportResponse {
  airports: [Airport!]!
  meta: PaginationMeta!
}

type UserProfile {
  id: ID!
  email: String!
  role: UserRole!
  emailVerified: Boolean!
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""User role in the system"""
enum UserRole {
  SUPER_ADMIN
  AIRPORT_ADMIN
  STAFF
  PASSENGER
}

type Flight {
  id: ID!
  flightNumber: String!
  departureAirportId: ID!
  destinationAirportId: ID!
  departureTime: DateTime!
  arrivalTime: DateTime!
  airline: String!
  availableSeats: Int!
  status: FlightStatus!
  createdAt: DateTime!
  updatedAt: DateTime!
}

"""Current status of a flight"""
enum FlightStatus {
  ON_TIME
  DELAYED
  CANCELED
}

type Staff {
  id: ID!
  userId: ID!
  airportId: ID!
  role: StaffRole!
  assignedFlightId: ID
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum StaffRole {
  PILOT
  CREW
  GROUND_STAFF
  SECURITY
}

type StaffResponse {
  staffs: [Staff!]!
  meta: PaginationMeta!
}

type AirportAdmin {
  id: ID!
  userId: ID!
  airportId: ID!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type AirportAdminResponse {
  airportAdmins: [AirportAdmin!]!
  meta: PaginationMeta!
}

type FlightResponse {
  flights: [Flight!]!
  meta: PaginationMeta!
}

type Query {
  getNotifications(input: QueryInput!): NotificationResponse!
  airports(input: AirportQueryInput = {}): AirportResponse!
  airport(id: ID!): Airport!

  """Return the currently authenticated user profile."""
  profile: UserProfile!
  staffs(input: StaffQueryInput = {}): StaffResponse!
  staff(id: ID!): Staff!
  airportAdmins(input: AirportAdminQueryInput = {}): AirportAdminResponse!
  airportAdmin(id: ID!): AirportAdmin!
  flight(id: ID!): Flight!
  flights(query: FlightQueryInput = {}): FlightResponse!
}

input QueryInput {
  page: Int
  limit: Int
  is_read: Boolean
}

input AirportQueryInput {
  page: Int
  limit: Int
}

input StaffQueryInput {
  page: Int
  limit: Int
}

input AirportAdminQueryInput {
  page: Int
  limit: Int
}

input FlightQueryInput {
  page: Int
  limit: Int
  departureTimeFrom: DateTime
  departureTimeTo: DateTime
  destinationAirportId: ID
  airline: String
}

type Mutation {
  markAsRead(id: String!): Notification!
  createAirport(input: CreateAirportInput!): Airport!
  updateAirport(id: ID!, input: UpdateAirportInput!): Airport!
  deleteAirport(id: ID!): Boolean!

  """Register a new passenger account. Does not issue tokens."""
  register(input: RegisterInput!): UserProfile!

  """Authenticate with email and password."""
  login(input: LoginInput!): UserProfile!

  """Rotate the refresh token stored in the httpOnly cookie."""
  refreshToken: UserProfile!

  """Verify email address using the OTP sent during registration."""
  verifyEmail(email: String!, otp: String!): Boolean!
  createStaff(input: CreateStaffInput!): Staff!
  updateStaff(id: ID!, input: UpdateStaffInput!): Staff!
  deleteStaff(id: ID!): Boolean!
  assignStaffToFlight(staffId: ID!, flightId: ID!): Staff!
  createAirportAdmin(input: CreateAirportAdminInput!): AirportAdmin!
  deleteAirportAdmin(id: ID!): Boolean!
  createFlight(input: CreateFlightInput!): Flight!
  updateFlight(id: ID!, input: UpdateFlightInput!): Flight!
  deleteFlight(id: ID!): Boolean!
}

input CreateAirportInput {
  name: String!
  code: String!
  city: String!
  country: String!
}

input UpdateAirportInput {
  name: String
  code: String
  city: String
  country: String
}

input RegisterInput {
  email: String!
  password: String!
}

input LoginInput {
  email: String!
  password: String!
}

input CreateStaffInput {
  email: String!
  password: String!
  airportId: ID
  role: StaffRole!
}

input UpdateStaffInput {
  role: StaffRole
}

input CreateAirportAdminInput {
  email: String!
  password: String!
  airportId: ID
}

input CreateFlightInput {
  flightNumber: String!
  departureAirportId: ID!
  destinationAirportId: ID!
  departureTime: DateTime!
  arrivalTime: DateTime!
  airline: String!
  availableSeats: Int!
  status: FlightStatus
}

input UpdateFlightInput {
  flightNumber: String
  departureAirportId: ID
  destinationAirportId: ID
  departureTime: DateTime
  arrivalTime: DateTime
  airline: String
  availableSeats: Int
  status: FlightStatus
}

type Subscription {
  notificationCreated: Notification!
  FLIGHT_STATUS_UPDATED(flightId: ID!): Flight!
}